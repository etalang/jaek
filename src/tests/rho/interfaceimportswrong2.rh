use interfaceimportsright
use interfaceimportsconflict


record Point {
    x : int
    y : int
}

main(args: int[][]) {
    p : Point
    p = Point(1, 2)
    p1 : Point = p
    println(unparseInt(p.x))
    println(unparseInt(p1.y))
}