use io
use conv

f() : int {
    c : bool = true
    x : bool = true
    if (c & x) {
        a : int = 5
        b : int = a + 1
        print(unparseInt(b))
        return b
    }

    return 17
}

main(args : int[][]) {
    str : int[] = {1,2,3}
    print("hello")
    x : int = f()
}