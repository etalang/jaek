use io
use conv
main(args : int[][]) {
    i : int = 3
    c : int = 4
    b : bool = true
    if (b) {
        c = c + 1
    } else {
        while(b) {
           b = false
           a : int = 1
           i = i * i
        }
        h : int = 5
    }
    print(unparseInt(3))
    return
}