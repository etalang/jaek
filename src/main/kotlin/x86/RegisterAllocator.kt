package x86

class RegisterAllocator {
    // TODO: allocate registers
    fun allocateRegisters(insns : List<Instruction>) : List<Instruction>{
        // all of the registers are correct in this
        return emptyList()
    }
}