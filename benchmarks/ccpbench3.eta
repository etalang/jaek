use io
use conv

main(args:int[][]) {
    thing : int = 5; thing2 : int = 6
    incr : int = 350000000
    z : int
    while (incr > 0) {
        x :int = 1
        y : int
        if (x<2) {
          y = 5+x
        } else {
          y = x
        }
        z = y*y
        incr = incr - 1
    }
    print(unparseInt(z))
}