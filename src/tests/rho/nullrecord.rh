use io
use conv

swap(p: Point) {
    t:int = p.x
    p.x, _ = p.y, 0
    p.y = t
}

record Point {
    x, y: int
}

main(args: int[][]) {
    p : Point
    if (p == null){
      println("record p initialized to null")
    }
}